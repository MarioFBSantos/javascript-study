function numbers(l = [1,2,'a','b'] ){

  const reg = numberPattern = /\d+/g;

  console.log(l.filter(nmb => typeof nmb === "number"));
    return l.filter(nmb => typeof nmb === "number"
);
}

numbers();